language: c++

sudo: true

before_script:
  - sudo add-apt-repository ppa:kubuntu-backports -y
  - sudo add-apt-repository ppa:ubuntu-toolchain-r-test -y
  - sudo apt-get update -q
  - sudo apt-get install -q qt4-x11 gcc-4.8

script:
        - qmake -recursive STA.pro
        - make
